<%= form_for @donation do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

	<%= f.label "Qual é o valor do seu investimento?" %><br>
  <%= f.text_field :financial_value, placeholder: "R$" %>

  <%= f.hidden_field :user_id, :value => @donation.user_id %>
  <%= f.hidden_field :idea_id, :value => @donation.idea_id %>

  <%= f.submit "Investir" %>
<% end %>
