<% if object.errors.any? %>
  <div>
    <div>
      O formulário não preenchido está adequadamente. Verifique os campos abaixo:
    </div>
    <ul>
    <% object.errors.messages.values.each do |msg| %>
        <% msg.each do  |m| %>
          <li><%= m %></li>
        <%end %>
    <% end %>
    </ul>
  </div>
<% end %>