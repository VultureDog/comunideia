<header>
  <div>
    <div>
      <%= link_to "Comunideia", root_path %>
      <nav>
        <ul>
          <li><%= link_to "Home",    root_path %></li>
          <% if signed_in? %>
            <li>
              <li><%= link_to current_user.name, '#' %></li>
              <li><%= link_to "Configuracoes", edit_user_path(current_user) %></li>
              <!--<li><%#= link_to "Configurações", '#' %></li>-->
              <li><%= link_to "Sign out",  signout_path, method: "delete" %></li>
            </li>
          <% else %>
            <%= form_for(:session, url: sessions_path) do |f| %>

              <%= f.label "E-mail" %>
              <%= f.text_field :email %>

              <%= f.label "Senha" %>
              <%= f.password_field :password %>

              <%= f.submit "Acessar" %>

              <%= link_to 'Registrar-se', signup_path %>
            <% end %>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>